{
  "name": "testagent",
  "version": "1.0.0",
  "description": "Autonomous GitHub issue resolution agent",
  "private": true,
  "workspaces": [
    "backend"
  ],
  "scripts": {
    "test": "npx jest --passWithNoTests && npx playwright test --reporter=list",
    "test:unit": "npx jest --passWithNoTests",
    "test:e2e": "npx playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
